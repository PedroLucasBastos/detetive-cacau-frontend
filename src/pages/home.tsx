import HomeMap from '../components/map/HomeMap';

function Home() {
    return (
        <main className="w-full flex-1 flex flex-col relative">
            <HomeMap />
        </main>
    );
}

export default Home;
